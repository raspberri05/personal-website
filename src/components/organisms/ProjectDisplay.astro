---
import { projects as p } from "@/content/data/projects.json";
import Project from "@/components/molecules/Project.astro";
---

{
  p.map((project) => (
    <>
      <Project
        name={project.name}
        description={project.description}
        href={project.link}
        ctext={project.custom?.text}
        clink={project.custom?.link}
        icons={project.icons}
        date={project.date}
      />
      <br />
      <br />
    </>
  ))
}
